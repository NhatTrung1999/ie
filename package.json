{
  "name": "ie-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "IE Video CT — Monorepo (Online + Offline)",
  "scripts": {
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",

    "build:backend": "cd backend && npm run build",
    "build:backend:sqlite": "cd backend && npm run prisma:generate:sqlite && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:electron": "cd electron && npm run build",

    "build:offline": "npm run build:backend:sqlite && npm run build:frontend && npm run build:electron",

    "dist:win": "cd electron && npm run dist:win",
    "dist:mac": "cd electron && npm run dist:mac",

    "prisma:sqlite:generate": "cd backend && npx prisma generate --schema=prisma/schema.sqlite.prisma",
    "prisma:sqlite:push": "cd backend && npx prisma db push --schema=prisma/schema.sqlite.prisma"
  }
}
